// YOUR CODE BELOW
function rotateArray(arr, num){
    let newArr = [];
    if(num < 0){
      //end goes in the front
          arr.pop().unshift()
    } else{
      //front goes on the end
     
    }
    return newArr;
  }


  console.log( rotateArray(['one', 'two', 'three', 'four'], 1));
  console.log(rotateArray(['first', 'second', 'third', 'fourth'], -3));